<%= provide(:title, @user.fullname) %>
<div class="row">
  <div class="col-md-3">
    <p><%= @user.fullname %></p>
    <p><%= @user.username %></p>
    <p><%= @user.email %></p>
  </div>
  <div class="col-md-9">
    Some features or histories would be shown here.
  </div>
</div>