<% if @appointment.is_open? && !current_user.is_employer? %>
<div id="request_form">
  <% if @appointment.applicant_include?(current_user) %>
    <%= render 'cancel_request' %>
  <% else %>
    <%= render 'request' %>
  <% end %>
</div>
<% end %>